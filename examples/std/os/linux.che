if const @static_assert(@is_os("linux"), "linux.che can only be loaded on linux") {}

timeval :: struct {
    tv_sec  : int // seconds
    tv_usec : int // and microseconds
}

gettimeofday :: (tv: ^timeval, tz: ^void) -> i32;

sleep :: (seconds: u32) -> u32;