trait Hashable {
    fn hash(ref Self) -> uint;
}

// TODO: move to string.che
impl Hashable for c_string {
    fn hash(ref Self) -> uint {
        let h: u32 = 2166136261

        while let i = 0; self[i] != '`0'; i += 1 {
            h += cast(u32) self[i] // TODO: change to xor
            h *= 16777619
        }

        return cast h
    }
}

impl Hashable for int {
    fn hash(ref Self) -> uint {
        return cast(uint) (self * 123)
    }
}
