

#load("ast_expr")

Stmt :: struct {
    concrete    : StmtType
}

StmtType :: enum {
    Func    : FunctionDecl
}

impl Printable for Stmt {
    print :: (ref Self, str: ref String, format: string) {
        use StmtType
        match concrete {
            Func($func) -> {
                str.appendf("fn {}() ``{``}", func.name)
            }
        }
    }
}

FunctionDecl :: struct {
    name    : string
}
