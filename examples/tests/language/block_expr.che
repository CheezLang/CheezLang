#test_expect_output("1, 2, 3, 4")


c_printf :: (format: c_string) #varargs #linkname("printf");

Main :: () {
    a := { 1 }
    b := {
        2
    }
    c := {
        x := 3
        x
    }
    d := {
        d := 4
        d
    }

    c_printf("%d, %d, %d, %d", a, b, c, d)
}
