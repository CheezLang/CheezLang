// #load("test2")
#load("std:io/io")
#load("std:iterator")
#load("std:random")

fn Main() {
    // new style
    // foreach! v in range(1, 3) {
        //println(v)
    // }
    // println("")

    // let rand = cast(IDistribution(double)) new StandardContinuousUniform{}
    // foreach! x in rand::samples()::take(5) {
//        println(x)
    // }
    // println("")

    // let arr = [9, 8, 7, 6, 5, 4, 3, 2, 1, 0]
    //foreach! i in range(0, arr.length - 1) println(arr[i])

    // foreach! i in range(1, 3) println(i)

    let a = 123
    println(format! "dec: "{a}"`nhex: "{a:x}"`nbin: "{a:b})
}

fn foo(a: $T, b: $T2) {
    println(a)
    println(b)
}
