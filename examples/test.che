#test_expect_output("5, 6, 5, 6, 5, 6 | 7, 8, 7, 8, 7, 8", "lol")
#test_expect_output("uiae")

//#load("std:io/io")
// #load("examples/trait_function_call_performance")
// #load("examples/glfw3_example")
// #load("examples/signed_distance_field_renderer")

fn c_printf(format: c_string) #varargs #linkname("printf");
// fn c_puts(str: c_string) #linkname("puts");

// fn println(str: c_string) {
//     c_puts(str)
// }

let a = (5, 6)
let b = a
let (x, y) = b

let (x2, y2) = b2
let b2 = a2
let a2 = (7, 8)

fn Main() {
    c_printf("%d, %d, %d, %d, %d, %d | %d, %d, %d, %d, %d, %d`n", a[0], a[1], b[0], b[1], x, y, a2[0], a2[1], b2[0], b2[1], x2, y2)
    c_printf("lol`n")
    c_printf("uiae`n")
}
