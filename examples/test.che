//#load("std:io/io")
// #load("examples/trait_function_call_performance")
// #load("examples/glfw3_example")
// #load("examples/signed_distance_field_renderer")

let a = swap(1, 2)
let (b, c) = swap(a[0], a[1])

fn swap(a: int, b: int) -> (int, int) {
    return (b, a)
}

fn first(t: (int, int)) -> int {
    return t[0]
}

fn Main() -> int {
    return swap(b, c)[1]
}
