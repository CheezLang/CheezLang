// #load("tests/language/local_variables.che")
#load("test2")
#load("std:math")

typedef LCG = LinearCongruentialGenerator

fn Main() {
    let rand = LCG::create()

    while let i = 0; i < 10; i += 1 {
        c_printf("%f`n", rand.next_double())
    }
}