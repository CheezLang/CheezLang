#load("compiler/main.che")

fn Main()
{
    let filename = "D:\Programming\CheezLang\examples\example_1.che"

    // RunCompiler(filename)

    let ints: int[] = MallocSlice(25)
    defer FreeSlice(ints)


    Println(ints)

    let i = 0
    while i < ints.length {
        defer i = i + 1
        ints[i] = i * i
    }

    Println(ints)
}


