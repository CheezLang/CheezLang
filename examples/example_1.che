#load("io.che")
#load("array.che")
//#load("memory.che")

struct Test {
    arr: int[5]
    curr: int
}

fn Main()
{
    let t: Test
    t.arr = [ 1, 2, 3, 4, 5 ]
    t.curr = 0

    use t
    while curr < 5 {
        PrintInt(arr[curr])
        Println("")
        curr = curr + 1
    }
}
