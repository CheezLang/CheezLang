#load("io.che")
#load("std_heap_allocator.che")

fn Main() {
    let s: String
    s.Init(new StdHeapAllocator {})
    Println(s)

    s.AppendString("hi wie gehts?")
    Println(s)

    Sprintf(&s, " lol, du lappen: {i32}", 123)
    Println(s)

    Sprintfn(&s, " ein vector: ({i32}, {i32}, {i32})", [(any)4, 5, 6])
    Println(s)
}
