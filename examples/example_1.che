// #load("io.che")
#load("std_heap_allocator.che")

struct AString(CharType: type) {
    data: CharType&
    length: int
    capacity: int
    allocator: Allocator
}

fn Main() {
    let s: AString(char)
    s.allocator = new StdHeapAllocator {}
}
