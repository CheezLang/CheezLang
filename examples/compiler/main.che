#load("../stdio.che")
#load("../string.che")
#load("../file.che")
#load("lexer.che")
#load("token.che")

fn RunCompiler(filename: string) {
    let lexer: Lexer
    lexer.Init(filename)

    while true {
        let t = lexer.NextToken()


    }

    lexer.Cleanup()
}
